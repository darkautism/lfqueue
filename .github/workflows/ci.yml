name: C/C++ CI

on:
  push:
    branches: [ "HP" ]
  pull_request:
    branches: [ "HP" ]

jobs:
  build:

    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
    - uses: actions/checkout@v4
    - name: make
      run: make
    - name: make test
      run: make test
